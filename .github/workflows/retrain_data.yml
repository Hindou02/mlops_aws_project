name: Retrain - DVC Data Pipeline

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "Raison du retraining"
        required: false
        default: "Mise à jour des données"

  push:
    branches:
      - main
    paths:
      - '**/*.dvc'
      - 'dvc.lock'

jobs:
  retrain:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure SSH key
        run: |
          mkdir -p ~/.ssh
          printf "%s" "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/ec2_key.pem
          chmod 600 ~/.ssh/ec2_key.pem

      - name: Debug host and SSH port
        run: |
          echo "Testing host: ${{ secrets.EC2_HOST }}"
          nc -zv ${{ secrets.EC2_HOST }} 22

      - name: Run DVC pipeline and retrain on EC2
        run: |
          ssh -vvv -o ConnectTimeout=20 -o StrictHostKeyChecking=no -i ~/.ssh/ec2_key.pem ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << 'ENDSSH'
            export AWS_ACCESS_KEY_ID="${{ secrets.AWS_ACCESS_KEY_ID }}"
            export AWS_SECRET_ACCESS_KEY="${{ secrets.AWS_SECRET_ACCESS_KEY }}"
            export AWS_DEFAULT_REGION="${{ secrets.AWS_DEFAULT_REGION }}"
            export MLFLOW_TRACKING_URI="${{ secrets.MLFLOW_TRACKING_URI }}"

            cd ~/mlflow
            nohup pipenv run mlflow server \
              --host 0.0.0.0 \
              --port 5000 \
              --default-artifact-root s3://24266-buckets \
              --allowed-hosts "*" \
              --cors-allowed-origins "*" > mlflow.log 2>&1 &

            cd ~/mlops_aws_project
            git pull origin main

            python3 -m pip install --user "dvc[s3]" || true

            export PATH="$HOME/.local/bin:$PATH"

            dvc pull
            dvc repro

            echo "Pipeline DVC terminé — nouveau modèle enregistré dans MLflow"

            sudo docker build -t mlflow-model-api .

            sudo docker stop api_container 2>/dev/null || true
            sudo docker rm api_container 2>/dev/null || true

            sudo docker run -d \
              --name api_container \
              -p 8000:8000 \
              -e AWS_ACCESS_KEY_ID="$AWS_ACCESS_KEY_ID" \
              -e AWS_SECRET_ACCESS_KEY="$AWS_SECRET_ACCESS_KEY" \
              -e AWS_DEFAULT_REGION="$AWS_DEFAULT_REGION" \
              -e MLFLOW_TRACKING_URI="$MLFLOW_TRACKING_URI" \
              mlflow-model-api
          ENDSSH

      - name: Health check
        run: |
          ssh -o StrictHostKeyChecking=no -i ~/.ssh/ec2_key.pem ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << 'ENDSSH'
            for i in {1..12}; do
              echo "Tentative $i..."
              if curl --fail http://localhost:8000/; then
                echo "API OK"
                exit 0
              fi
              sleep 10
            done
            echo "API non disponible après plusieurs tentatives"
            exit 1
          ENDSSH